<div class="col-sm-2">
  <% if user_signed_in? %>
    <h5>Chatrooms</h5>
    <ul>
      <% current_user.chatrooms.each do |chatroom| %>
        <li><%= link_to chatroom.name, chatroom %></li>
      <% end %>
    </ul>
  <% end %>
</div>

